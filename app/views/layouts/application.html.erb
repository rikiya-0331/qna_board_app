<!DOCTYPE html>
<html>
<head>
  <title>Q&A App</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
</head>
<body class="flex flex-col min-h-screen">

  <header class="bg-gray-800 text-white p-4 shadow-md">
    <div class="container mx-auto flex justify-between items-center">
      <div class="text-2xl font-bold">
        <%= link_to "Q&A App", root_path, class: "text-white hover:text-gray-300" %>
      </div>

      <nav>
        <ul class="flex space-x-4">
          <li><%= link_to "ホーム", root_path, class: "hover:text-gray-300" %></li>
          <li><%= link_to "クイズを始める", quiz_start_path, class: "hover:text-gray-300" %></li>
          <% if user_signed_in? %>
            <li><%= link_to "マイページ", mypage_path, class: "hover:text-gray-300" %></li>
            <li><%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete }, class: "hover:text-gray-300" %></li>
          <% else %>
            <li><%= link_to "新規登録", new_user_registration_path, class: "hover:text-gray-300" %></li>
            <li><%= link_to "ログイン", new_user_session_path, class: "hover:text-gray-300" %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </header>

  <div id="flash" class="container mx-auto px-4 mt-4">
    <% flash.each do |type, message| %>
      <div class="p-3 mb-2 rounded-md text-sm <% case type when 'notice' %>bg-green-100 text-green-800<% when 'alert' %>bg-red-100 text-red-800<% else %>bg-blue-100 text-blue-800<% end %>">
        <%= message %>
      </div>
    <% end %>
  </div>

  <main class="flex-grow">
    <%= yield %>
  </main>

  <footer class="bg-gray-800 text-white p-4 text-center mt-auto shadow-inner">
    <div class="container mx-auto">
      <p>&copy; <%= Time.current.year %> Q&A App. All rights reserved.</p>
    </div>
  </footer>

</body>
</html>
